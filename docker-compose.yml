version: "3.9"

services:

  api:
    build: .
    ports:
      - "8000:8000"

  streamlit:
    build: .
    command: streamlit run streamlit_app/app.py --server.port 8501
    ports:
      - "8501:8501"
    depends_on:
      - api